## Running Tests 

This project uses `pytest` for unit testing core logic.

### Run all unit tests

```bash
python -m pytest tests/
```

Notes

GUI / Playwright tests are excluded from unit test runs

Core tests are fast and deterministic
